/* ===================================================
   LIGHT GUITAR TUTOR — Main Menu USS
   Dark game UI aesthetic
   =================================================== */

/* ---- ROOT / SCREENS ---- */
.screen {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(18, 18, 24, 1);
}

.screen-content {
    flex-grow: 1;
    padding: 40px 60px;
    align-items: center;
    justify-content: center;
}

.center-content {
    justify-content: center;
    align-items: center;
}

/* ---- TYPOGRAPHY ---- */
.game-title {
    color: rgb(255, 255, 255);
    font-size: 64px;
    -unity-text-align: upper-center;
    -unity-font-style: bold;
    letter-spacing: 6px;
    margin-bottom: 48px;
}

.screen-title {
    color: rgb(220, 220, 230);
    font-size: 36px;
    -unity-font-style: bold;
    -unity-text-align: upper-center;
    margin-bottom: 32px;
    letter-spacing: 4px;
}

/* ---- BUTTONS (SHARED) ---- */
.menu-button {
    width: 260px;
    height: 56px;
    font-size: 20px;
    -unity-font-style: bold;
    border-radius: 12px;
    border-width: 2px;
    border-color: rgba(255, 255, 255, 0.12);
    background-color: rgba(40, 40, 56, 1);
    color: rgb(210, 210, 220);
    margin: 8px 0;
    transition-duration: 0.15s;
}

.menu-button:hover {
    background-color: rgba(60, 60, 80, 1);
    border-color: rgba(255, 255, 255, 0.3);
    scale: 1.04;
}

.menu-button:active {
    background-color: rgba(30, 30, 46, 1);
    scale: 0.97;
}

.primary-button {
    background-color: rgba(56, 120, 255, 1);
    border-color: rgba(100, 160, 255, 0.4);
    color: rgb(255, 255, 255);
}

.primary-button:hover {
    background-color: rgba(80, 140, 255, 1);
    border-color: rgba(130, 180, 255, 0.6);
}

.danger-button {
    background-color: rgba(180, 50, 50, 0.85);
    border-color: rgba(220, 80, 80, 0.4);
    color: rgb(255, 220, 220);
}

.danger-button:hover {
    background-color: rgba(210, 60, 60, 1);
}

.back-button {
    width: 180px;
    height: 48px;
    font-size: 18px;
    margin-top: 24px;
    background-color: rgba(50, 50, 66, 1);
}

.button-group {
    align-items: center;
}

/* ===================================================
   MODE SELECT — POSTER CARDS
   =================================================== */
.poster-grid {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    margin-top: 12px;
    margin-bottom: 24px;
}

.poster-card {
    width: 220px;
    height: 280px;
    margin: 12px;
    border-radius: 16px;
    border-width: 2px;
    border-color: rgba(255, 255, 255, 0.08);
    background-color: rgba(30, 30, 44, 1);
    align-items: center;
    justify-content: flex-start;
    padding: 0;
    overflow: hidden;
    transition-duration: 0.18s;
}

.poster-card:hover {
    scale: 1.06;
    border-color: rgba(255, 255, 255, 0.25);
    background-color: rgba(38, 38, 56, 1);
}

.poster-card:active {
    scale: 0.96;
}

.poster-icon-area {
    width: 100%;
    height: 140px;
    border-top-left-radius: 14px;
    border-top-right-radius: 14px;
}

.tutorial-color {
    background-color: rgba(72, 190, 120, 0.85);
}

.slow-color {
    background-color: rgba(80, 160, 240, 0.85);
}

.live-color {
    background-color: rgba(240, 80, 80, 0.85);
}

.scales-color {
    background-color: rgba(200, 160, 60, 0.85);
}

.poster-label {
    color: rgb(240, 240, 245);
    font-size: 22px;
    -unity-font-style: bold;
    -unity-text-align: upper-center;
    margin-top: 18px;
}

.poster-desc {
    color: rgba(180, 180, 200, 0.8);
    font-size: 14px;
    -unity-text-align: upper-center;
    margin-top: 6px;
}

/* ===================================================
   SONG SELECT
   =================================================== */
.song-header-row {
    flex-direction: row;
    align-items: center;
    justify-content: center;
    width: 100%;
    margin-bottom: 8px;
}

.song-header-row .screen-title {
    margin-bottom: 0;
    margin-right: 16px;
}

.mode-badge {
    font-size: 16px;
    -unity-font-style: bold;
    color: rgb(255, 255, 255);
    background-color: rgba(56, 120, 255, 0.9);
    border-radius: 8px;
    padding: 4px 16px;
    height: 30px;
    -unity-text-align: middle-center;
}

.song-list {
    flex-grow: 1;
    width: 700px;
    max-height: 480px;
    background-color: rgba(24, 24, 36, 1);
    border-radius: 12px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.06);
    margin-top: 8px;
    margin-bottom: 16px;
    padding: 8px;
}

/* Song row item (built at runtime) */
.song-row {
    flex-direction: row;
    align-items: center;
    height: 56px;
    padding: 0 16px;
    margin: 4px 0;
    border-radius: 8px;
    background-color: rgba(36, 36, 52, 1);
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.04);
    transition-duration: 0.12s;
}

.song-row:hover {
    background-color: rgba(50, 50, 72, 1);
    border-color: rgba(255, 255, 255, 0.15);
}

.song-row-selected {
    background-color: rgba(56, 120, 255, 0.25);
    border-color: rgba(56, 120, 255, 0.6);
}

/* Hide the default ListView selection highlight so our custom class takes over */
.song-list .unity-list-view__item--selected {
    background-color: rgba(0, 0, 0, 0);
}

.song-name {
    flex-grow: 1;
    color: rgb(230, 230, 240);
    font-size: 18px;
    -unity-font-style: bold;
    overflow: hidden;
    text-overflow: ellipsis;
}

.song-artist {
    width: 160px;
    color: rgba(170, 170, 190, 0.9);
    font-size: 15px;
    -unity-text-align: upper-center;
}

.song-duration {
    width: 70px;
    color: rgba(150, 150, 170, 0.8);
    font-size: 14px;
    -unity-text-align: upper-center;
}

.difficulty-badge {
    min-width: 60px;
    height: 24px;
    border-radius: 6px;
    font-size: 13px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    color: rgb(255, 255, 255);
    padding: 0 10px;
    margin-left: 8px;
}

.difficulty-easy {
    background-color: rgba(72, 190, 120, 0.9);
}

.difficulty-medium {
    background-color: rgba(220, 170, 40, 0.9);
}

.difficulty-hard {
    background-color: rgba(220, 60, 60, 0.9);
}

.song-footer-row {
    flex-direction: row;
    justify-content: center;
    width: 100%;
}

.song-footer-row .menu-button {
    margin: 0 12px;
}

/* ===================================================
   INFO SCREEN (Tutorial & Scales)  —  side-by-side
   =================================================== */
.info-screen-content {
    flex-grow: 1;
    padding: 32px 48px;
    align-items: center;
    justify-content: center;
}

.info-body {
    flex-direction: row;
    align-items: stretch;
    background-color: rgba(26, 26, 38, 1);
    border-radius: 18px;
    border-width: 2px;
    border-color: rgba(255, 255, 255, 0.08);
    overflow: hidden;
    max-width: 1440px;
    width: 95%;
    max-height: 1231px;
}

/* Left side — large image */
.info-image {
    flex-grow: 1;
    flex-basis: 0;
    min-width: 55%;
    background-image: url("project://database/Assets/RomitMadeStuff/Images/scales.png");
    -unity-background-scale-mode: scale-to-fit;
    background-color: rgba(20, 20, 30, 1);
    border-right-width: 2px;
    border-right-color: rgba(255, 255, 255, 0.06);
}

/* Right side — text panel */
.info-text-panel {
    flex-basis: 0;
    flex-grow: 0;
    width: 340px;
    min-width: 300px;
    padding: 40px 32px;
    align-items: flex-start;
    justify-content: flex-start;
}

.info-title {
    color: rgb(230, 230, 245);
    font-size: 32px;
    -unity-font-style: bold;
    letter-spacing: 4px;
    margin-bottom: 12px;
}

.info-divider {
    width: 60px;
    height: 3px;
    background-color: rgba(56, 120, 255, 0.8);
    border-radius: 2px;
    margin-bottom: 24px;
}

.info-text {
    color: rgba(190, 190, 210, 0.92);
    font-size: 17px;
    -unity-text-align: upper-left;
    white-space: normal;
    line-height: 170%;
    flex-grow: 1;
}

/* Scale-type buttons (left side of info screen) */
.scales-button-group {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 16px 12px;
    min-width: 120px;
    background-color: rgba(22, 22, 34, 1);
    border-right-width: 2px;
    border-right-color: rgba(255, 255, 255, 0.06);
}

.scale-type-button {
    width: 110px;
    height: 52px;
    font-size: 17px;
    -unity-font-style: bold;
    border-radius: 10px;
    border-width: 2px;
    border-color: rgba(255, 255, 255, 0.1);
    background-color: rgba(40, 40, 56, 1);
    color: rgb(210, 210, 220);
    margin: 8px 0;
    letter-spacing: 2px;
    transition-duration: 0.15s;
}

.scale-type-button:hover {
    scale: 1.06;
    border-color: rgba(255, 255, 255, 0.3);
}

.scale-type-button:active {
    scale: 0.95;
}

.major-btn {
    background-color: rgba(56, 120, 255, 0.85);
    border-color: rgba(100, 160, 255, 0.4);
    color: rgb(255, 255, 255);
}

.major-btn:hover {
    background-color: rgba(80, 140, 255, 1);
    border-color: rgba(130, 180, 255, 0.6);
}

.minor-btn {
    background-color: rgba(160, 70, 200, 0.85);
    border-color: rgba(190, 110, 230, 0.4);
    color: rgb(255, 255, 255);
}

.minor-btn:hover {
    background-color: rgba(180, 90, 220, 1);
    border-color: rgba(210, 140, 240, 0.6);
}

.blues-btn {
    background-color: rgba(200, 140, 40, 0.85);
    border-color: rgba(230, 180, 70, 0.4);
    color: rgb(255, 255, 255);
}

.blues-btn:hover {
    background-color: rgba(220, 160, 50, 1);
    border-color: rgba(240, 200, 90, 0.6);
}

/* ===================================================
   OPTIONS SCREEN
   =================================================== */
.options-content {
    align-items: center;
    max-width: 600px;
    align-self: center;
}

.option-row {
    flex-direction: row;
    align-items: center;
    width: 520px;
    margin: 10px 0;
}

.option-label {
    width: 180px;
    color: rgb(200, 200, 215);
    font-size: 18px;
    -unity-font-style: bold;
}

.option-slider {
    flex-grow: 1;
}

.option-slider .unity-base-slider__dragger {
    background-color: rgba(56, 120, 255, 1);
    border-radius: 6px;
    width: 18px;
    height: 18px;
}

.option-slider .unity-base-slider__tracker {
    background-color: rgba(60, 60, 80, 1);
    border-radius: 4px;
    height: 6px;
}

.option-toggle {
    margin-left: 4px;
}

.option-dropdown {
    flex-grow: 1;
    background-color: rgba(36, 36, 52, 1);
    border-radius: 8px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.1);
    color: rgb(210, 210, 220);
    font-size: 16px;
    height: 36px;
}
